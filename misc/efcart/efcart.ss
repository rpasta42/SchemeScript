

(define (gallery-task

(define (main-task)
   (gallery-task))


(define (f x y)
   (+ (* x x) (+ y y)))

(+ (f 3 5) 10)

(define (on_ck) (alert "clicked"))

(tag div
   (attr (onclick "on_ck()"))
   (tag div (attr (id "test")) "nested tag")
   "main tag")


